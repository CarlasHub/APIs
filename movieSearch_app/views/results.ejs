<h1>Result</h1>
<% for(var i = 0 ; i < data["Search"].length ;i++){%>
   <li>
       <%= data["Search"][i]["Title"] %>
   </li>
 <% } %>
 
  <% data['Search'].forEach(function(movie){ %>
     
    <li><%= movie['Title'] %></li>
     
 <% }); %>
 
